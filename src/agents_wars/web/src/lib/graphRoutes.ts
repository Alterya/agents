export type GraphRouteMap = Record<string, `/${string}`>;

// Connected to Task 4 (Landing page). Type: frontend/a11y
export const graphRouteMap: GraphRouteMap = {
  n1: "/hub",
  n2: "/scale",
  n3: "/promptbro",
};
